doctype html
html   
  head
    title WEB
    link(rel='stylesheet', href='/cookies.css')    
  body    
      h1.main_title #{isAuth ? "로그인 됨" : "로그인 안됨"}
      form(action=`${isAuth ? "/logout_process" : "/login"}`, method=`${isAuth ? "post" : "get"}`) 
        button #{isAuth ? "로그아웃" : "로그인"}
      